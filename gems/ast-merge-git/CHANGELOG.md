# Changelog

## Unreleased

### Added

- Add executable `merge2` benchmark support through the shared provider
  contract, paired with an explicit naive template-overwrite baseline.

### Fixed

- Report semantic correctness, source preservation, and expected parse
  rejections independently in local benchmark results and hard gates.
