[tools]
ruby = "4.0.2"

[env]
KJ_HOMEPAGE_URI = "https://structuredmerge.org"

[tasks.test]
description = "Run Ruby specs"
run = "bundle exec rspec"

[tasks.check]
description = "Run the local Ruby verification suite"
run = "bundle exec rspec"
